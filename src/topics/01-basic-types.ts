let name: string = 'Strider';
let hpPoints: number | 'FULL' = 95;
let reign: string | number = 'New York';
const isAlive: boolean = true;

hpPoints = 'FULL';

console.log({ name, hpPoints, reign, isAlive });

export {};
